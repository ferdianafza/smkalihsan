<%= form_with(model: artikel, local: true) do |form| %>
  <% if artikel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artikel.errors.count, "error") %> prohibited this artikel from being saved:</h2>

      <ul>
        <% artikel.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :thumbnail %>
    <%= form.file_field :thumbnail %>
  </div>

  <div class="field">
    <%= form.label :judul %>
    <%= form.text_field :judul %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.rich_text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
